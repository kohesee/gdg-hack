services:
  gemini-cli:
    build:
      context: .
      dockerfile: Dockerfile.cli
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-pro}
    stdin_open: true
    tty: true

